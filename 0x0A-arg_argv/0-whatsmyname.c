#include "holberton.h"

/**
 * main - program that prints its name
 *
 * @argc: arguement count
 * @argv: array of pointers for the argument
 *
 * Return: 0 if successful and 1 if not
 *
 */

int main(int argc, int *argv[])
{
	argc = argc;
	printf("%s\n", *argc[]);

	return (0);
}
