#include <stdio.h>

/**
 * main - prints natural numbers below 1024 that are multipliers of 3 or 5
 *
 *Return: 0 (success)
 *
 */

int main(void)
{
	int j, k;

	for (j = 1; j < 1024; j++)
	{
		if ((a % 3) == 0 || (a % 5) == 0)
		{
			k = k + j;
		}

	}

	printf("%d\n", k);
	return (0);

}
